export default "API_KEY_GOES_HERE";
/**
  Save this file as `key.js` in the src/cloudkit directory.
  The exported string should contain the API key for the cloudkit container. 
  Having this file in .gitignore DOES NOT prevent the end user from seeing this key,
  but it does keep the key out of our github repo during development.
*/
